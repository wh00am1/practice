#include <iostream>
#include <list>
using namespace std;

list<int> l;
int n, m, t, k;
void solve(){
	for(int i=1; i<=n; i++){
		cin >> t;
		l.push_front(t);
	}
	for(int i=)
}
int main(int argc, char const *argv[])
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	while(cin >> n >> m)
		solve();
	return 0;
}