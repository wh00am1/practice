#include <iostream>
using namespace std;
using ll = long long int;

ll t, n, a[51];

int main(int argc, char const *argv[])
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> t;
	while(t--){
		cin >> n;
		if(n>50)	cin.ignore();	cout << "NO" << endl;
		else{
			for(int i=0; i<n; i++){	cin >> a[i];}
			for(int i=0; i<)
		}
	}
	return 0;
}